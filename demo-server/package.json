{
  "name": "@jokester/sio-serverless-demo-server",
  "version": "0.0.1",
  "description": "a simple socket.io server",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "gts lint",
    "clean": "gts clean",
    "dev": "DEBUG=limb:*,-socket.io:* nodemon --exec ts-node-transpile-only main-node.ts",
    "dev:node": "DEBUG=limb:*,-socket.io:* tsx watch src/node/main.ts",
    "build": "node build.mjs",
    "deploy:cf": "wrangler deploy",
    "fix": "gts fix"
  },
  "author": "Wang Guan <momocraft@gmail.com>",
  "license": "BSD",
  "dependencies": {
    "@jokester/ts-commonutil": "^0.6.0",
    "debug": "^4.3.4",
    "hono": "^4.2.2",
    "socket.io": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "*",
    "esbuild": "*",
    "tsx": "*",
    "typescript": "*",
    "wrangler": "*"
  }
}
